git commit -m <descr>

    -a stage all the previously staged files/content (not including a new files)

git commit --ammend
    open the $EDITOR and add more commit info
    or just add a little bit about the previous commit

