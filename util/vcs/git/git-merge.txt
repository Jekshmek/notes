grep -r'<<<<' *
    search for all the merge conflict markers


git config --global merge.tool
git config --global diff.tool

git merge
    --no-ff
        do not do a fast-forward (for one-commit merges)
    --ff-only
        only do a fast-forward

git checkout <branch-to-merge>
git merge <feature-branch>
... --//-- the same stuff in the opposite direction
git chechout <featur-branch>
git merge <branch-to-merge>
    fast-forward merge
git push...


git checkout master
git fetch

git merge <remote-name>/<branch>
git pull

git merge <issue>-<description>
git merge --no-ff <issue>-<desc>
